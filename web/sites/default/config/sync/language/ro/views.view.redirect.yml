display:
  default:
    display_options:
      exposed_form:
        options:
          submit_button: Filtru
      filters:
        redirect_redirect__uri:
          expose:
            label: Către
