display:
  default:
    display_options:
      exposed_form:
        options:
          submit_button: Filtrera
      filters:
        redirect_redirect__uri:
          expose:
            label: Till
