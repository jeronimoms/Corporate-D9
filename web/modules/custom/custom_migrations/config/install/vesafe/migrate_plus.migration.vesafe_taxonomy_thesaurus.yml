# Migration file for creating "Taxonomy terms Section" nodes from BBDD.
#
# This file contains much of the information which we
# used to specify in the "Migration" class in Drupal 7.
# But why write code if you can write YAML?
# This is how we declare a Migration the Drupal 8 way.

##################### BASIC INFORMATION ###################

id: vesafe_taxonomy_thesaurus
label: 'Terms: Thesaurus'
audit: true
migration_group: vesafe_migrate
deriver: Drupal\taxonomy\Plugin\migrate\D7TaxonomyTermDeriver

##################### BASIC INFORMATION ###################



########################## SOURCE #########################

source:
  plugin: d7_taxonomy_term
  bundle: thesaurus

########################## SOURCE #########################



########################## PROCESS ########################

process:
  # If you are using this file to build a custom migration consider removing
  # the tid field to allow incremental migrations.
  tid:
    -
      plugin: get
      source: tid
  vid:
    -
      plugin: default_value
      default_value: thesaurus
  name:
    -
      plugin: get
      source: name_field
  langcode:
    -
      plugin: get
      source: language
  'description_field/value':
    -
      plugin: get
      source: description_field
  field_thesaurus_code:
    - plugin: get
      source: field_thesaurus_code
  # Only attempt to stub real (non-zero) parents.
  parent_id:
    -
      plugin: skip_on_empty
      method: process
      source: parent
    -
      plugin: migration_lookup
      migration: vesafe_taxonomy_thesaurus
  parent:
    -
      plugin: default_value
      default_value: 0
      source: '@parent_id'
########################## PROCESS ########################



######################## DESTINATION ######################

destination:
  plugin: entity:taxonomy_term

######################## DESTINATION ######################



######################## DEPENDENCIES #####################

# We specify that this migration depends on the unops_project_migrate module.
# Based on this dependency, this migration will be removed when the
# custom_migrate module is uninstalled.
dependencies:
  enforced:
    module:
      - custom_migrations

######################## DEPENDENCIES #####################
